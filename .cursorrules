# 86,400 — Cursor 規則

完整說明看 CLAUDE.md，動手前請先讀。以下幾條無論如何都不要違反：

1. 單檔、無建置、無相依。整個產品就是 index.html。不要引入框架、
   打包工具、npm 套件或後端。雙擊 index.html 就要能跑。
2. 顏色一律用 CSS 變數，不要寫死 hex。整組色票由 applyPressure()
   每秒依「今天過掉多少」寫進 :root，寫死的顏色深夜不會跟著變。
3. 設定裡的「夜間強度」開關是這個設計方向唯一的煞車。關掉時顏色、
   閃爍、抖動、文案全部要回到平靜狀態。不能移除，也不能只關一半。
4. 介面文字只有正體中文與英文，絕不出現日文。不說「浪費時間」——
   沒追蹤的時間叫「未命名時間」。沒有 streak、沒有分數、不跟別人比較。
5. 進行中的區塊只用 S.activeId 記 id，block 本體只存在 S.days 裡。
   不要再存第二份，重新整理後會脫鉤並遺失紀錄。
6. 改完要實際開瀏覽器跑一次，確認 console 無錯誤、重新整理後資料還在。

驗收預覽：index.html?preview=23:40 可把時鐘固定在指定時刻檢查各壓力階段。
