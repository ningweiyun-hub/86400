# 86,400 — 主題「日夜兩面」配色需求單

**給設計。** 程式端的機制已經做完並驗證過，現在缺的只有色票。
需要交付的是 **4 組色票**（見下面第 4 節的表），每組 6 個 OKLCH 色值。

---

## 1. 這是什麼

每個主題現在只有一張臉。要改成**同一個主題有日間與夜間兩面** —— 不是換成
另一個主題，是同一個主題的兩種光線。「文青簡約」白天是米白紙、晚上是深墨紙，
但它還是文青簡約。

**切換時機由系統決定，不用做開關。** iOS／macOS 的「外觀 → 自動（日落到日出）」
本來就是照裝置位置算的，網頁讀 `prefers-color-scheme` 就等於跟著日落走。
設定頁另外給了「跟隨系統／日間／夜間」三段，方便使用者鎖定與我們驗收。

---

## 2. 顏色系統：只有兩軌，其餘都是算出來的

每一面只需要定義 **3 個壓力節點 × 2 軌顏色 = 6 個色值**，全部用 **OKLCH**
（不要給 HEX 或 sRGB —— 中段內插會變濁，這條不能改）。

```
stops: [
  { at: 0,    bg:[L,C,H], neon:[L,C,H] },   // 平靜
  { at: 0.55, bg:[L,C,H], neon:[L,C,H] },   // 中段
  { at: 1,    bg:[L,C,H], neon:[L,C,H] }    // 最濃
]
```

- **`bg`** = 頁面底色。**`neon`** = 重點色（大數字、按鈕、光暈、進度火花）。
- `L` 明度 0–1｜`C` 彩度 0–0.4｜`H` 色相 0–360。
- 三點之間**線性內插**，中間色自動算出來，不用給。

### `at` 是什麼

`at` 是壓力值 `p`，代表「今天燒掉多少」：

```
p = clamp((今天過掉的小時數 − 9) / 14, 0, 1) ^ 2.2
```

| p | 大約時刻 | 感覺 |
|---|---|---|
| 0 | 09:00 以前 | 平靜、日子還很長 |
| 0.55 | 約 20:20 | 開始不安 |
| 1 | 午夜 | 燒完了 |

**重要：`at:0` 那一組必須自己就能成立。** 使用者關掉「夜間強度」時 `p` 恆為 0，
整天都停在這一組色上 —— 它是這個主題的「平靜臉」，不是只出現三小時的過場。

### 設計**不需要**給的東西

文字色、分隔線、填色、面板底、商店配色、光暈半徑全部是從 `bg`／`neon`／`light`
自動算出來的。給錯反而會被覆蓋。

---

## 3. 每一面另外三個參數

| 參數 | 意義 | 現有值參考 |
|---|---|---|
| `light` | **true＝這是日間那一面**（墨色 `#1c1a17`）／false＝夜間（墨色 `#e9edf4`）。決定所有文字與線條的明暗方向。 | 文青 true，其餘 false |
| `glow` | 光暈強度倍率 0–1。0 是完全不發光。 | 霓虹 1｜科技 0.85｜迷彩 0.35｜文青 0.25 |
| `scan` | 掃描線強度 0–1。0 是沒有掃描線。 | 霓虹 1｜科技 0.7｜迷彩 0.55｜文青 0 |

---

## 4. 要交付的四組

| 主題 | id | 已經有的那一面 | **要補的那一面** |
|---|---|---|---|
| 焦慮霓虹 | `neon` | 夜間 | **日間** |
| 文青簡約 | `literati` | 日間 | **夜間** |
| 科技 | `tech` | 夜間 | **日間** |
| 迷彩 | `camo` | 夜間 | **日間** |

### 現有色票（當作對照，不要改）

```
neon（夜）      at 0    bg [0.16, 0.030, 150]   neon [0.86, 0.20, 140]
                at 0.55 bg [0.15, 0.050,  55]   neon [0.84, 0.17,  72]
                at 1    bg [0.12, 0.100,  15]   neon [0.70, 0.26,  20]

literati（日）  at 0    bg [0.96, 0.012,  85]   neon [0.42, 0.045, 145]
                at 0.55 bg [0.94, 0.018,  70]   neon [0.38, 0.055,  55]
                at 1    bg [0.91, 0.028,  40]   neon [0.34, 0.070,  30]

tech（夜）      at 0    bg [0.14, 0.040, 260]   neon [0.82, 0.16, 210]
                at 0.55 bg [0.13, 0.055, 280]   neon [0.78, 0.18, 195]
                at 1    bg [0.11, 0.080, 300]   neon [0.72, 0.22, 330]

camo（夜）      at 0    bg [0.22, 0.048, 128]   neon [0.74, 0.12, 122]
                at 0.55 bg [0.17, 0.055,  92]   neon [0.70, 0.13,  78]
                at 1    bg [0.13, 0.062,  48]   neon [0.68, 0.15,  62]
```

### 交付格式

一組一段，直接照這個形狀寫（我會原封不動貼進 `SKINS` 的 `alt` 欄位）：

```js
// neon 的日間那一面
alt: {
  light: true, glow: 0.5, scan: 0.3,
  stops: [
    { at:0,    bg:[?,?,?], neon:[?,?,?] },
    { at:0.55, bg:[?,?,?], neon:[?,?,?] },
    { at:1,    bg:[?,?,?], neon:[?,?,?] }
  ]
}
```

給表格或 Figma 標註也可以，只要六個值是 OKLCH 三元組就行。

---

## 5. 硬性限制

1. **升溫的紅是刻意的，不要拿掉。** 這個 App 的核心就是讓人坐立難安 ——
   顏色從綠走到琥珀走到熱紅，是「時間在燒」的敘事，不是錯誤警告。
   日間那一面一樣要有這條升溫路徑，只是發生在淺色底上。
2. **`at:0` 要平靜。** 見上面第 2 節。
3. **對比要過得去。** 正文對底色至少 4.5:1，次要說明文字至少 3:1。
   我這邊有可跑的掃描器，交來之後我會把四個主題 × 五個畫面 × 日夜兩面全掃一次
   再回報。不用先自己算。
4. **中段要檢查。** 只給三個點，但實際會用到中間的內插值。
   驗收時用 `index.html?preview=HH:MM` 把時鐘固定在 12:00 / 18:00 / 20:20 / 23:40
   各看一次。
5. **同一個主題的兩面要看得出是同一個主題。** 色相家族與個性要延續，
   不能日間版看起來像另一個產品。

---

## 6. 目前不跟著日夜換的東西（已知，先不處理）

- PWA 的 App 圖示與 manifest 主題色寫死是 `#101a16`（開機時產生一次）。
- 商店的金幣金色是貨幣語意，只依 `light` 在 `#FFC24B` / `#8A6212` 之間切。

---

## 7. 程式端現況

已經做完並驗證：`skinOf()` 會依系統外觀自動回傳正確的那一面；系統在日落翻面時
`matchMedia` 監聽器會即時重繪；設定頁有三段切換；`S.appearance` 有存檔與相容處理。

**沒有 `alt` 的主題兩面完全相同**（回傳同一個物件），所以在色票補上來之前，
現在的外觀一模一樣不會變。色票補進去就會自己生效，不用再改程式。
